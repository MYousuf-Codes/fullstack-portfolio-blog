import React from 'react';

const Hero: React.FC = () => {
    return (
        <main>

            {/* Slider */}
            <div>
            // here will be a slider with a library and also will have the paignations, each slider will have the 3 posts as, first one will be a rectangle and other two will be half in first' width and also small in size, all will be fetched from the sanity cms. The slider will be functional and responsive on all devices, inclding desktop, tablet and mobile, even the larger LEDs.

            </div>

            {/* Logos */}
            <div>
                // here a line of technologis logos continuosly moving form right to left and pause on hover. This will contains the logos of html, css, typescript, javascript, python, react, nextjs, tailwind css, sanity, stripe, auth.js, ship engine, shadcn ui, material ui, radix ui. This will be a professional, fucnitonal, and responsive on all over devices.
            </div>
        </main>
    );
};

export default Hero;